{
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "commands": {
      "build": "npm install && npm run build"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
